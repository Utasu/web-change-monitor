[project]
name = "web-change-monitor"
version = "1.0.0"
description = "Safely monitor authorized HTTPS pages for meaningful changes"
license = { text = "MIT" }
requires-python = ">=3.11"

[project.urls]
Repository = "https://github.com/Utasu/web-change-monitor"

[tool.ruff]
line-length = 100
